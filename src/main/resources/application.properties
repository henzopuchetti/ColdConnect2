spring.application.name=coldconnect

# Ativa o perfil prod
spring.profiles.active=prod

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Porta para Railway ou local
server.port=${PORT:8080}
